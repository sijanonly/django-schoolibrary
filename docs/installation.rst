============
Installation
============

At the command line::

    $ easy_install django_schoolibrary

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django_schoolibrary
    $ pip install django_schoolibrary
